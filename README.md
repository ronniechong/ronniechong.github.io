# ronniechong.github.io

Terminal-style interactive portfolio. React + Vite + xterm.js, deployed to
GitHub Pages via GitHub Actions on every push to `master`.

## Development

```
npm install
npm run dev
```

## Commands

`help`, `whoami`, `ls pages/`, `ls projects/`, `cat <page>`, `open <project>`, `stats`, `contact`
